apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "espresso-review.fullname" . }}-test-connection"
  labels:
    {{- include "espresso-review.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "espresso-review.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
